#include<stdio.h>

int main(void)
{
    int a, b, add;
    printf("Enter the first number:");
    scanf("%d", &a);
    printf("Enter the second number:");
    scanf("%d", &b);
    add = a + b;
    printf("Addition :%d", add);
}